---
description: "الرسالة القابلة للترجمة نفسها بصيغة % و.format() وسلاسل $ في flufl.i18n وt-string، بما في ذلك كيفية ربط كل منها للقيم والتعامل مع كتالوج تالف."
---

# لماذا t-strings؟

أربع طرق لإدخال قيمة في رسالة قابلة للترجمة، مقارنةً على الجملة نفسها.
الخلاصة المختصرة:

- مع **صيغة %**، يتحول حذف مترجم لحرف واحد إلى انهيار في بيئة الإنتاج.
- مع **str.format**، تستطيع الترجمة قراءة خصائص الكائنات التي تمررها
  شيفرتك — بما فيها الأسرار.
- مع **سلاسل `$`** (flufl.i18n)، تُسحب القيم ضمنياً من متغيرات الدالة
  المستدعية، وتصل العناصر النائبة المنقوطة إلى الخصائص أيضاً.
- مع **t-strings**، يبقى التنسيق في شيفرتك، وتُفحص الترجمات وقت التشغيل،
  ويتراجع الكتالوج المعطوب إلى نص المصدر بدلاً من الانهيار.

بقية هذه الصفحة هي الدليل على ذلك، طريقةً تلو الأخرى.

!!! note "ثلاثة أطراف تلمس كل رسالة مترجمة"

    **الكتالوج** هو ملف الترجمات — بصيغة `.po` أثناء تحرير البشر له،
    ويُجمّع إلى `.mo` كي يحمّله التطبيق (يمر
    [الدرس التعليمي](tutorial.md) بكليهما). ثلاثة أطراف تلمس كل رسالة:
    **المطوّر** يكتب نص المصدر، و**المترجم** يحرر الكتالوج — على منصة
    خارجية في الغالب، بعيداً عن أي مراجعة للشيفرة — و**التطبيق** يعرض
    الاثنين معاً وقت التشغيل. يجيب كل أسلوب تنسيق أدناه عن السؤال نفسه
    بطريقة مختلفة: *ما المقدار الذي ينبغي أن يتحكم فيه الكتالوج من لغة
    التنسيق؟* في الأمثلة، `_` هو الاسم المتعارف عليه لدالة الترجمة،
    و`tr` هو اسم هذه المكتبة.

## التنسيق بعلامة % { #-format }

```python
_("Hello %(name)s") % {"name": name}
```

ما الذي قد يسوء: حذف حرف واحد في ترجمة يُسقط العرض.

يحمل نص الكتالوج صيغة printf، بما فيها حرف نوع أخير — حرف `s` في
`%(name)s` — يسهل إغفاله ويسهل إتلافه:

```pycon
>>> "Hello %(name)" % {"name": "Ada"}  # the trailing "s" was deleted
Traceback (most recent call last):
  ...
ValueError: incomplete format
```

يصبح تعديل حرف واحد في محرر PO تتبّع استثناء في بيئة الإنتاج. يكتشف
`msgfmt --check-format` من GNU ذلك، لكن فقط للرسائل الموسومة
`python-format` وعندما يمر الكتالوج فعلاً عبر msgfmt في طريقه إلى تطبيقك.

## str.format { #strformat }

```python
_("Hello {name}").format(name=name)
```

يزيل حرف النوع الأخير مع إبقاء العنصر النائب مسمّى وقابلاً لإعادة الترتيب
بحرية. أما ما قد يسوء فينتقل إلى الجانب الآخر من التبادل: تكتسب الترجمة
سلطة على كائناتك.

`str.format` لغة تعبيرات صغيرة، واستدعاؤها على نص يعني منح ذلك النص حق
استخدام هذه اللغة:

```pycon
>>> "{name.__class__.__mro__}".format(name="Ada")
"(<class 'str'>, <class 'object'>)"
>>> settings.api_key = "sk-live-…"
>>> "{conf.api_key}".format(conf=settings)
'sk-live-…'
```

الآن استبدل بتلك النصوص الحرفية ما تعيده `_()`. إذا عادت ترجمة
`Hello {name}` على شكل `{conf.api_key}`، فإن عرضها يطبع مفتاح API الخاص
بك — الكتالوج، لا شيفرتك، هو من قرر ما الذي يُقرأ. الكتالوج ليس شيفرة،
لكنه يسافر كما تسافر البيانات: إلى منصة ترجمة، عبر أيدٍ عديدة، عائداً
بصيغة `.po`، مجمّعاً إلى `.mo`، وأحياناً مورَّداً من خارج مشروعك كلياً.
تمنح `.format()` كل محطة في تلك الرحلة حق الوصول إلى خصائص الكائنات التي
تمررها.

## سلاسل `$` وflufl.i18n { #-strings-and-flufli18n }

```python
from flufl.i18n import initialize

_ = initialize("example")

name = "Ada"
print(_("Hello $name"))  # Hello Ada — the value came from the caller's locals
```

توفر المكتبة القياسية لغة الاستيفاء `$name` عبر [`string.Template`][stdlib-template]، لكنها
ليست بحد ذاتها API للترجمة. يجمع [`flufl.i18n`][flufl-i18n] هذا الأسلوب مع البحث في
كتالوجات gettext. لاحظ أن القيمة لا تُمرر أبداً: يبني flufl.i18n نطاق
أسماء الاستبدال من المتغيرات العامة والمحلية للمستدعي — فأي متغير موجود
عند موضع الاستدعاء يصبح متاحاً للرسالة. وتكون لخريطة `extras` الاختيارية
الأولوية على كليهما. لا تتضمن الصيغة الموجهة للمترجم حرف نوع أخيراً أو
محدد تنسيق، وتبقى العناصر النائبة قابلة لإعادة الترتيب بحرية.

لا يثير الاستبدال غير المتاح استثناءً. مع `name = "Ada"` وعدم وجود `nombre`
في نطاق أسماء المستدعي، تُعرض ترجمة الكتالوج `Hello $nombre` على شكل
`Hello $nombre`، فيبقى العنصر النائب غير المحلول ظاهراً. يحافظ هذا
[السلوك الموثق] على بقية الرسالة المترجمة بدلاً من إفشال الاستدعاء. ومع ذلك،
قد تستمر الاستثناءات المثارة أثناء حل خاصية أو تحويل قيمة في الانتشار.

يتفوق `flufl.i18n` في جانب ذي صلة على `string.Template` المجرد. يقبل
[Template المخصص] الخاص به عناصر نائبة منقوطة مثل `$settings.api_key`،
ويحل [المترجم] تلك المسارات بالنسبة إلى قيم المستدعي. يمكن لعنصر نائب مترجم
تسمية أي متغير محلي أو عام متاح للمستدعي، واجتياز خصائصه باستخدام صيغة النقاط.
وهذا مناسب عندما تحتاج الرسالة إلى خاصية، لكنه يجعل إطار المستدعي أيضاً جزءاً
من نطاق أسماء الاستبدال للكتالوج. تصف المقارنة أدناه `flufl.i18n` 6.0.0،
لا كل استخدام ممكن لـ`string.Template`.

## t-strings { #t-strings }

```python
tr(t"Hello {name}")
```

لا يزال الكتالوج يرى `Hello {name}` ويبقى كتالوج PO/MO عادياً. الفرق هو ما
*يُسمح* للترجمة أن تقوله، ومن الذي يتحقق من ذلك.

تفحص هذه المكتبة كل ترجمة مقابل عناصر الرسالة المصدر النائبة قبل العرض،
ولا تقبل إلا الأسماء البسيطة. مقابل `t"Hello {name}"`:

| ما تحتويه الترجمة | سبب الرفض |
| --- | --- |
| `{name.__class__.__mro__}` | placeholder `{name.__class__.__mro__}` must be a plain name, copied from the source message unchanged |
| `{name!r}` | placeholder `{name}` adds formatting; write `{name}` on its own, because the source message decides how the value is formatted |
| `{0}` | placeholder `{0}` must be a plain name, copied from the source message unchanged |
| `{nombre}` | translation does not match the source placeholders: `{name}` is missing; `{nombre}` is not in the source message |

الرفض لا يعني الانهيار: تسجل المكتبة افتراضياً تحذيراً وتعرض نص المصدر،
فلا يُسقط كتالوج سيئ التطبيق أبداً —
[وهو العقد نفسه الذي يحافظ عليه gettext](guide.md#what-happens-when-a-catalog-is-wrong).

يبقى التنسيق حيث كُتب، في الشيفرة:

```python
amount = 1234.5
tr(t"Total: {amount:,.2f}")  # msgid is "Total: {amount}"
```

لا تصل `:,.2f` إلى الكتالوج أبداً، فلا تستطيع أي ترجمة تغييرها، ولا يضطر
أي مترجم إلى النظر إليها.

فرق أخير هو الأدوات: t-strings صيغة جديدة، ولذلك يتطلب استخراجها إلى
`.pot` حالياً مستخرجاً يدرك t-string، مثل الذي
[توفره هذه الحزمة لـBabel](extraction.md).

## مقارنة مباشرة { #side-by-side }

| | `%(name)s` | `.format()` | `flufl.i18n` `$name` | `t"…"` |
| --- | --- | --- | --- | --- |
| هل العنصر النائب مسمّى؟ | نعم | نعم | نعم | نعم |
| هل يمكن للمترجم إعادة ترتيب العناصر النائبة؟ | نعم | نعم | نعم | نعم |
| من أين تأتي القيم؟ | خريطة صريحة | وسائط صريحة | متغيرات المستدعي المحلية والعامة، مع `extras` اختيارية | القيم الملتقطة داخل t-string |
| هل يستطيع الكتالوج تغيير طريقة تنسيق قيمة؟ | نعم | نعم | لا | لا |
| هل يستطيع الكتالوج النفاذ إلى الكائنات (الوصول إلى الخصائص)؟ | لا | نعم | نعم، بأسماء منقوطة | لا |
| ترجمة *تحذف* عنصراً نائباً — ماذا يُعرض؟ | تختفي القيمة بصمت | تختفي القيمة بصمت | تختفي القيمة بصمت | نص المصدر، مع تحذير ([افتراضياً](guide.md#what-happens-when-a-catalog-is-wrong)) |
| ترجمة *تضيف* عنصراً نائباً مجهولاً — ماذا يُعرض؟ | استثناء | استثناء | يبقى العنصر النائب ظاهراً كنص | نص المصدر، مع تحذير ([افتراضياً](guide.md#what-happens-when-a-catalog-is-wrong)) |
| هل تُفحص العناصر النائبة وقت العرض؟ | لا | لا | لا | نعم (انظر أدناه) |
| ما علامة PO التي يستنتجها Babel كي تتحقق الأدوات الحالية؟ | `python-format` | `python-brace-format` | لا توجد | `python-brace-format` |
| يستخدم كتالوجات PO/MO عادية؟ | نعم | نعم | نعم | نعم |
| يحتاج إلى مستخرج مصدر مخصص؟ | لا | لا | لا | نعم، حالياً |

عن الفحص وقت العرض: تُفحص الرسائل المفردة بحثاً عن تطابق تام للعناصر
النائبة. وتُفحص رسائل الجمع أيضاً، وفق
[قاعدة الاتحاد والتقاطع](spec.md) التي تسمح لصيغ جمع اللغة الهدف بأن
تختلف عن صيغ المصدر؛ أما الفحص الأدق لكل صيغة فيجري عند تجميع الكتالوجات
([الاستخراج](extraction.md)).

يتعلق صف علامة التنسيق بالتحقق المدرك للعناصر النائبة، لا بتوافق الكتالوج.
تعني `لا توجد` أن أدوات gettext القياسية لا تزال تقرأ الرسالة وتجمعها، لكن
`msgfmt --check-format` لا يملك قواعد لعناصر `$` النائبة كي يطبقها.

## التكلفة { #what-it-costs }

لا يمكن استخدام f-string بهذه الطريقة مطلقاً؛ فحين تراها أي مكتبة تكون نصاً
مكتملاً بالفعل، ولذلك فإن ترجمتها تعني ترجمة جزء. تُبقي t-strings
([PEP 750]) النص الثابت والقيم منفصلين مع الحفاظ على صيغة تشبه f-string
وربط القيم صراحةً. توفر سلاسل `$` بالفعل بديلاً موجزاً بنموذج مختلف للربط
والفشل. إن `flufl.i18n` حزمة ناضجة تعمل على Python 3.10 وما بعده؛ أما
`gettext-tstrings` فهي حالياً في مرحلة alpha، ولأن t-strings صيغة جديدة
فهي تتطلب Python 3.14 أو أحدث.

الكلفة الأخرى هي القيد نفسه: يجب أن يكون الاستيفاء اسماً بسيطاً.

```python
tr(t"Hello {user.name}")  # raises InvalidTemplateError at the call site
```

```python
name = user.name  # compute it first
tr(t"Hello {name}")
```

هذا قيد حقيقي. وإلى جانب ربط القيم في المصدر وفحص العناصر النائبة وقت التشغيل،
يمنع نصوص الكتالوج من تقييم التعبيرات ويحافظ على أسماء عناصر نائبة ذات معنى.

أما كيف وصلت Python إلى مفترق الطرق هذا — مقترحا PEP بينهما عشر سنوات،
ونقاش المكتبة القياسية الذي أُغلق دون إجابة — فتُروى القصة مع مصادرها في
صفحة [الخلفية](background.md).

  [PEP 750]: https://peps.python.org/pep-0750/
  [stdlib-template]: https://docs.python.org/3/library/string.html#template-strings
  [flufl-i18n]: https://flufli18n.readthedocs.io/en/stable/
  [السلوك الموثق]: https://flufli18n.readthedocs.io/en/stable/using.html#substitutions-and-placeholders
  [Template المخصص]: https://gitlab.com/flufl/flufl.i18n/-/blob/6.0.0/src/flufl/i18n/_substitute.py
  [المترجم]: https://gitlab.com/flufl/flufl.i18n/-/blob/6.0.0/src/flufl/i18n/_translator.py
