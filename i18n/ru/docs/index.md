---
description: "Безопасный перевод полных сообщений t-string через gettext и Babel без передачи форматирования в каталог."
---

# gettext-tstrings

Безопасная интеграция t-строк Python 3.14+ с gettext и Babel.

```python
import gettext

from gettext_tstrings import Translator

_ = Translator(gettext.translation("messages", localedir="locales"))
name = "Ada"
print(_(t"Hello {name}"))
```

В каталог попадает полное предложение `Hello {name}`. Перевод может переставить
или повторить `{name}`, но не может удалить его, добавить новый заполнитель или
собственное форматирование.

## Решаемая проблема

К моменту вызова библиотеки f-строка уже интерполирована, поэтому переводится
только фрагмент. t-строка ([PEP 750]) сохраняет отдельно статический текст,
вычисленные значения, исходные выражения, преобразования и спецификации формата
— именно такое разделение нужно каталогу сообщений. Различия с `%(name)s` и
`.format()` показаны в [сравнении](comparison.md).

gettext и Babel не определяют, как превратить t-строку в сообщение. Эта
библиотека фиксирует решение в [версионируемой спецификации](spec.md) и
поставляет [набор тестов на соответствие](spec.md#conformance).

## Принятые решения

- Переводить полные сообщения, а не части предложений.
- Разрешать только простые имена переменных, например `{name}`.
- Оставлять `!r` и `:.2f` под контролем приложения и вне каталога.
- Разрешать перестановку и повтор известных заполнителей, но не доступ к
  атрибутам и не добавление форматирования.
- Использовать обычные POT-, PO- и MO-файлы и существующие инструменты.

## Этот сайт использует библиотеку сам

Документация — не просто переведённая демонстрация. Навигация, подписи темы,
строка copyright и отчёт сборки с множественными формами выводятся из
PO-каталогов самой `gettext-tstrings`.
[Многоязычный builder](https://github.com/yhay81/gettext-tstrings/blob/main/scripts/build_multilingual_docs.py)
в каждой строгой сборке выполняет контекстные сообщения, именованные
заполнители и правила множественного числа всех десяти языков.

## Установка

```console
python -m pip install gettext-tstrings
```

Нужен Python 3.14 или новее. У рендеринга **нет зависимостей** — используется
только `gettext` из стандартной библиотеки.

Для извлечения и проверки каталогов нужен [Babel]. Установите extra в среде
разработки или CI:

```console
python -m pip install "gettext-tstrings[babel]"
```

## Что читать дальше

<div class="grid cards" markdown>

- **[Зачем нужны t-строки](comparison.md)** — одно сообщение тремя способами.
- **[Руководство](guide.md)** — API времени выполнения, язык запроса,
  отложенные строки и неверные каталоги.
- **[Извлечение](extraction.md)** — процесс `pybabel`, настройка и проверка.
- **[Спецификация](spec.md)** — стабильный контракт и тесты соответствия.
- **[API](api.md)** — все публичные экспорты пакета.

</div>

## Состояние

Проект находится на стадии alpha. Небольшой контракт и спецификация — его
стабильная часть; Python API до стабильного выпуска ещё может меняться. Нужны
новые языковые примеры, постоянные измерения производительности и опыт
реальных проектов на gettext и Babel.

[Issues и pull requests](https://github.com/yhay81/gettext-tstrings/issues)
приветствуются.

## Сообщество

- Выберите ограниченную
  [good first issue](https://github.com/yhay81/gettext-tstrings/issues?q=is%3Aissue%20state%3Aopen%20label%3A%22good%20first%20issue%22).
- Задавайте вопросы в
  [Q&A Discussions](https://github.com/yhay81/gettext-tstrings/discussions/categories/q-a).
- Обсуждайте идеи API в
  [Ideas Discussions](https://github.com/yhay81/gettext-tstrings/discussions/categories/ideas).
- Перед pull request прочитайте
  [руководство для участников](https://github.com/yhay81/gettext-tstrings/blob/main/CONTRIBUTING.md).

  [PEP 750]: https://peps.python.org/pep-0750/
  [Babel]: https://babel.pocoo.org/
