---
description: "Μεταφράστε ολόκληρα μηνύματα t-string μέσω gettext και Babel, με τις τιμές και τη μορφοποίηση να μένουν έξω από τον κατάλογο."
title: "gettext-tstrings"
hide:
  - navigation
  - toc
---

<div class="home-hero" markdown>

# Μεταφράστε ολόκληρα μηνύματα,<br>όχι κομμάτια συμβολοσειρών.

Το `gettext-tstrings` συνδέει τα t-strings της Python 3.14+ με τους τυπικούς
καταλόγους gettext και τα εργαλεία του Babel. Οι τιμές και η μορφοποίηση
μένουν στον κώδικα της εφαρμογής· ο κατάλογος κρατά ένα πλήρες μήνυμα με απλά
σύμβολα κράτησης θέσης `{name}`:

```python
import gettext

from gettext_tstrings import Translator

_ = Translator(gettext.translation("messages", localedir="locales"))
name = "Ada"
print(_(t"Hello {name}"))  # with a Japanese catalog: こんにちは Ada
```

[Ξεκινήστε την εκμάθηση :material-arrow-right:](tutorial.md){ .md-button .md-button--primary }
[Συγκρίνετε τις εναλλακτικές](comparison.md){ .md-button }

Alpha · Python 3.14+ · συνηθισμένοι κατάλογοι PO/MO · χωρίς εξαρτήσεις κατά την εκτέλεση
{ .home-facts }

Αυτός ο ιστότοπος εφαρμόζει όσα τεκμηριώνει: κάθε γλωσσική έκδοση —
η πλοήγηση, οι ετικέτες και η αναφορά build με επίγνωση πληθυντικού —
αποδίδεται από καταλόγους PO μέσω του
[ίδιου του `gettext-tstrings`](https://github.com/yhay81/gettext-tstrings/blob/main/scripts/build_multilingual_docs.py).
{ .home-hero-note }

</div>

## Είναι για εσάς; { #is-this-for-you }

**Ταιριάζει ήδη σήμερα όταν** η εφαρμογή σας τρέχει σε Python 3.14 ή
νεότερη· χρησιμοποιείτε ήδη gettext και Babel, ή θέλετε να υιοθετήσετε τη ροή
εργασίας PO/MO τους· και θέλετε σύνταξη t-string με επώνυμα σύμβολα κράτησης
θέσης που ελέγχονται πριν αποδοθούν.

**Δεν ταιριάζει ακόμη όταν** χρειάζεστε Python 3.13 ή παλαιότερη· απαιτείτε
σταθερό Python API — αυτή είναι έκδοση alpha, και η
[προδιαγραφή](spec.md) είναι το μέρος της που έχει σταθεροποιηθεί· ή σχεδόν
όλο το μεταφράσιμο κείμενό σας ζει σε μια γλώσσα προτύπων και όχι σε πηγαίο
κώδικα Python.

Έχετε ήδη καταλόγους; Εξακολουθούν να δουλεύουν. Το
`_("Hello {name}").format(name=name)` και το `tr(t"Hello {name}")` παράγουν το
ίδιο msgid, οπότε οι υπάρχουσες μεταφράσεις επιβιώνουν από τη μετάβαση — η
[Μετάβαση](migration.md) διατρέχει ολόκληρη τη μετακίνηση.

## Τι επιτρέπεται να πει ο κατάλογος { #what-the-catalog-may-say }

Ο κατάλογος παραλαμβάνει το πλήρες μήνυμα `Hello {name}`. Μια μετάφραση
μπορεί να αναδιατάξει ή να επαναλάβει το `{name}`, και μπορεί να ξαναγράψει
κάθε άλλη λέξη γύρω του. Δεν μπορεί να παραλείψει το σύμβολο κράτησης θέσης,
να επινοήσει καινούργιο, να φτάσει μέσα από αυτό στα αντικείμενά σας, ούτε να
προσθέσει δική της μορφοποίηση.

Αυτή είναι όλη η υπόσχεση: **μια μετάφραση δεν μπορεί να αλλάξει τη δομή του
μηνύματος που μεταφράζει.** Η βιβλιοθήκη το ελέγχει στην είσοδο — όταν
μεταγλωττίζονται οι κατάλογοι — και ξανά κατά την απόδοση· μια χαλασμένη
καταχώριση που φτάνει παρ' όλα αυτά στην παραγωγή καταγράφει μια
προειδοποίηση και αποδίδει το πηγαίο μήνυμα αντί να καταρρεύσει.

!!! note "Νέοι στο gettext; Όλη η ροή εργασίας σε τέσσερις προτάσεις"

    Το **gettext** είναι ο καθιερωμένος τρόπος με τον οποίο μεταφράζεται το
    λογισμικό, στην Python και πολύ πέρα από αυτήν. Ο κώδικάς σας επισημαίνει
    τα μεταφράσιμα μηνύματα· ένας *εξαγωγέας* τα συλλέγει σε ένα
    αρχείο-πρότυπο (`.pot`)· ένας μεταφραστής — συνήθως όχι προγραμματιστής —
    συμπληρώνει ένα αρχείο καταλόγου (`.po`) ανά γλώσσα, το οποίο
    μεταγλωττίζεται σε ένα δυαδικό `.mo` που η εφαρμογή σας φορτώνει κατά την
    εκτέλεση. Το καθιερωμένο όνομα της συνάρτησης μετάφρασης είναι `_`, οπότε
    το `_(t"Hello {name}")` διαβάζεται ως «μετάφρασε αυτό το μήνυμα». Η
    **[εκμάθηση](tutorial.md)** διατρέχει όλη τη διαδρομή — επισήμανση,
    εξαγωγή, μετάφραση, μεταγλώττιση, εκτέλεση — σε περίπου πέντε λεπτά.

## Το πρόβλημα που λύνει { #the-problem-it-solves }

Ένα f-string έχει ήδη υποστεί παρεμβολή τιμών μέχρι να το δει οποιαδήποτε
βιβλιοθήκη — το `f"Hello {name}"` έχει γίνει `"Hello Ada"`, και η μετάφραση
των τμημάτων γύρω από μια τιμή σπάει τη γραμματική των περισσότερων γλωσσών.
Ένα t-string ([PEP 750]) κρατά χωριστά το στατικό κείμενο, τις αποτιμημένες
τιμές, τις πηγαίες εκφράσεις, τις μετατροπές και τις προδιαγραφές
μορφοποίησης — που είναι ακριβώς ο διαχωρισμός που χρειάζεται ένας κατάλογος
μηνυμάτων.
[Τι αλλάζει αυτό](comparison.md), σε σύγκριση με τα `%(name)s`, `.format()`
και τις συμβολοσειρές `$`.

Τίποτα όμως στο gettext ή στο Babel δεν ορίζει πώς ένα t-string γίνεται
μήνυμα. Αυτή η βιβλιοθήκη κάνει αυτή την επιλογή, την καταγράφει ως
[εκδοσιοποιημένη προδιαγραφή](spec.md) και συνοδεύεται από τη
[σουίτα συμμόρφωσης](spec.md#conformance) για τον έλεγχό της.

## Οι κανόνες σχεδίασης { #the-design-rules }

- Μεταφράζει ολόκληρα μηνύματα, ποτέ αποσπάσματα προτάσεων.
- Δέχεται μόνο απλά ονόματα μεταβλητών όπως `{name}`.
- Κρατά τα `!r` και `:.2f` υπό τον έλεγχο της εφαρμογής, έξω από τον
  κατάλογο.
- Επιτρέπει στις μεταφράσεις να αναδιατάσσουν και να επαναλαμβάνουν γνωστά
  σύμβολα κράτησης θέσης, εμποδίζοντάς τες ταυτόχρονα να φτάσουν σε ιδιότητες
  ή να προσθέσουν μορφοποίηση.
- Επαναχρησιμοποιεί συνηθισμένα αρχεία POT, PO και MO, και τα εργαλεία που
  ήδη τα διαβάζουν.

Και ο αντίστοιχος κατάλογος όσων αφήνει σκόπιμα ήσυχα: δεν τοπικοποιεί
αριθμούς, νομίσματα ή ημερομηνίες — [μορφοποιήστε τα πρώτα](guide.md#locale-aware-values),
με το Babel· δεν κάνει escape την αποδιδόμενη έξοδο για HTML, για ένα κέλυφος
ή για ένα τερματικό· και δεν μπορεί να κρίνει αν μια μετάφραση είναι *σωστή*,
παρά μόνο αν τα σύμβολα κράτησης θέσης της είναι ανέπαφα.

## Εγκατάσταση { #install }

```console
python -m pip install gettext-tstrings
```

Απαιτείται Python 3.14 ή νεότερη. **Η απόδοση δεν έχει εξαρτήσεις** —
χρησιμοποιεί το `gettext` της τυπικής βιβλιοθήκης και τίποτα άλλο.

Η εξαγωγή και η επικύρωση καταλόγων περνούν μέσα από το [Babel], οπότε
εγκαταστήστε αυτό το extra όπου εκτελείται το `pybabel`, δηλαδή συνήθως σε
περιβάλλον ανάπτυξης ή CI και όχι σε εικόνα παραγωγής:

```console
python -m pip install "gettext-tstrings[babel]"
```

## Πού να πάτε στη συνέχεια { #where-to-go-next }

**Ξεκινήστε από εδώ** — χωρίς να προϋποτίθεται εμπειρία με το gettext:

<div class="grid cards" markdown>

- **[Εκμάθηση](tutorial.md)** — από έναν άδειο κατάλογο σε μια λειτουργική
  ιαπωνική μετάφραση σε πέντε βήματα, με κάθε εντολή να εμφανίζεται μαζί με
  την έξοδό της.
- **[Γιατί t-strings](comparison.md)** — το ίδιο μήνυμα γραμμένο με τέσσερις
  τρόπους, και τι παραδίδουν στον κατάλογο τα `%(name)s`, `.format()` και οι
  συμβολοσειρές `$`.

</div>

**Χρησιμοποιήστε το** — οι αναφορές εργασίας:

<div class="grid cards" markdown>

- **[Οδηγός](guide.md)** — το API χρόνου εκτέλεσης: ποιο σημείο εισόδου να
  χρησιμοποιήσετε, πληθυντικοί, γλώσσες ανά αίτημα, αναβαλλόμενες
  συμβολοσειρές, και τι συμβαίνει όταν ένας κατάλογος είναι λανθασμένος.
- **[Εξαγωγή](extraction.md)** — η αναφορά του `pybabel`: ρύθμιση,
  προσαρμοσμένα ονόματα συναρτήσεων, και πώς τα υπάρχοντα εργαλεία
  επικυρώνουν αυτούς τους καταλόγους δωρεάν.
- **[Στην παραγωγή](workflow.md)** — ο κύκλος όπως τον τρέχει μια ομάδα: ο
  κύκλος ενημέρωσης, οι καταχωρίσεις fuzzy, οι πύλες CI, οι πλατφόρμες
  μετάφρασης και η αποστολή.
- **[Μετάβαση](migration.md)** — υιοθέτησή του σε ένα έργο που έχει ήδη
  καταλόγους, ένα σημείο κλήσης τη φορά.
- **[Για μεταφραστές](translators.md)** — μία σελίδα για να τη δώσετε σε όποιον
  επεξεργάζεται τα αρχεία `.po`.

</div>

**Κατανοήστε το** — από την ιστορία ώς την υλοποίηση:

<div class="grid cards" markdown>

- **[Ιστορικό](background.md)** — γιατί υπάρχει αυτή η βιβλιοθήκη: τριάντα
  χρόνια gettext, δύο PEP, και η συζήτηση για την τυπική βιβλιοθήκη που
  έκλεισε χωρίς απάντηση.
- **[Παγίδες](pitfalls.md)** — τι έσπασε στην πραγματικότητα η μετάφραση αυτού
  του ιστότοπου σε τριάντα πέντε γλώσσες, και ποιο μισό μπορεί να πιάσει ένα
  εργαλείο.
- **[Πώς λειτουργεί](internals.md)** — από το αντικείμενο-πρότυπο του PEP 750
  ώς την αποδιδόμενη συμβολοσειρά, και οι κρυφές μνήμες που κάνουν τον έλεγχο
  φθηνό.

</div>

**Αναφορά** — τα συμβόλαια:

<div class="grid cards" markdown>

- **[API](api.md)** — όλα όσα εξάγει το πακέτο, σε μία σελίδα.
- **[Προδιαγραφή](spec.md)** — η σύμβαση t-string ↔ msgid ως σταθερό,
  εκδοσιοποιημένο συμβόλαιο, με μηχανικά αναγνώσιμη σουίτα συμμόρφωσης.

</div>

## Κατάσταση { #status }

| | |
| --- | --- |
| Έκδοση πακέτου | 0.1.0a7 |
| Σταθερότητα API | alpha — το Python API μπορεί ακόμη να αλλάξει |
| [Προδιαγραφή](spec.md) | v1, με [σουίτα συμμόρφωσης](spec.md#conformance) |
| Python | 3.14 και νεότερη· δοκιμασμένη σε 3.14, 3.14t (free-threaded) και 3.15 |
| Babel | 2.18 ή νεότερη, και μόνο όπου εκτελείται το `pybabel` |
| Εξαρτήσεις κατά την εκτέλεση | καμία — το `gettext` της τυπικής βιβλιοθήκης |
| Μορφή καταλόγων | συνηθισμένα POT, PO και MO |
| Αλλαγές | [CHANGELOG](https://github.com/yhay81/gettext-tstrings/blob/main/CHANGELOG.md) |

Έκδοση alpha. Το συμβόλαιο είναι σκόπιμα μικρό και η
[προδιαγραφή](spec.md) είναι το σταθερό του μέρος· το Python API μπορεί ακόμη
να αλλάξει. Πριν από μια σταθερή έκδοση χρειάζονται fixtures σε περισσότερες
γλώσσες, συνεχής παρακολούθηση επιδόσεων, αναθεώρηση του API από ανθρώπους
που χρησιμοποιούν στα σοβαρά τα gettext και Babel, και δοκιμές συμβατότητας
με κάθε υποστηριζόμενη έκδοση Python και Babel.

Τα [Issues και τα Pull Requests](https://github.com/yhay81/gettext-tstrings/issues)
είναι ευπρόσδεκτα — μια έκδοση alpha είναι ακριβώς η στιγμή που αξίζει ακόμη
να συζητηθεί η διεπαφή.

## Γίνετε μέλος της κοινότητας { #join-the-community }

- Διαλέξτε ένα
  [good first issue](https://github.com/yhay81/gettext-tstrings/issues?q=is%3Aissue%20state%3Aopen%20label%3A%22good%20first%20issue%22)
  για μια οριοθετημένη συνεισφορά.
- Κάντε ερωτήσεις χρήσης στα
  [Q&A Discussions](https://github.com/yhay81/gettext-tstrings/discussions/categories/q-a).
- Φέρτε ροές εργασίας gettext από την παραγωγή και ιδέες για το API στα
  [Ideas Discussions](https://github.com/yhay81/gettext-tstrings/discussions/categories/ideas).
- Διαβάστε τον
  [οδηγό συνεισφοράς](https://github.com/yhay81/gettext-tstrings/blob/main/CONTRIBUTING.md)
  πριν ανοίξετε ένα Pull Request.

  [PEP 750]: https://peps.python.org/pep-0750/
  [Babel]: https://babel.pocoo.org/
