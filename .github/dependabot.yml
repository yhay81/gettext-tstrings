version: 2
updates:
  # Actions are pinned to commit SHAs, which never resolve to a newer release
  # on their own; without this they would silently rot.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly

  - package-ecosystem: uv
    directory: "/"
    schedule:
      interval: weekly
